server:
  port: 8085
spring:
  application:
    name: "technology"
  devtools:
    add-properties: true
  profiles:
    include: null
management:
  endpoints:
    web:
      exposure:
        include: "health"
  endpoint:
    health:
      probes:
        enabled: true

entries:
  reactive-web:
    basePath: "/technology"